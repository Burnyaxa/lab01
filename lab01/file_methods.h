#pragma once
#include "country.h"
#include <iostream>
#include <fstream>
#include <vector>
#include <string>
using namespace std;

vector <string> rF();
string rC(int);
int rSum(int);
int getSize();
int getNum(int, int);
void getResultFile(vector <country> data, int size);
vector <int> cmdInput(int , char*[]);

